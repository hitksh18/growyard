import type { Metric } from "@/types/content";

/**
 * Results / Metriics.
 *
 * These are deliberately editable placeholders using `--` values so no
 * statistics are fabricated. Replace each with a real, verifiable figure
 * as GrowthYard's performance data becomes available.
 */
export const resultsMetrics: Metric[] = [
  { label: "Brands scaled", value: "--" },
  { label: "Revenue influenced", value: "--", suffix: "+" },
  { label: "Campaigns launched", value: "--" },
  { label: "Client retention", value: "--", suffix: "%" },
];
